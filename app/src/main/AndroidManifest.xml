<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ddpl.cipot">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logotrans"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logotrans"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SiswaHomeActivity" />
        <activity android:name=".SiswaProfilActivity" />
        <activity android:name=".IndikatorKemampuanActivity" />
        <activity
            android:name=".IndikatorKemampuanDetailActivity"
            android:windowSoftInputMode="stateHidden|adjustPan" />
        <activity android:name=".SiswaLaporanSemesterActivity" />
        <activity android:name=".GuruHomeActivity" />
        <activity android:name=".UpdateDataSiswaActivity"></activity>
    </application>

</manifest>